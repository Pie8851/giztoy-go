{
  "openapi": "3.0.3",
  "info": {
    "title": "Workflow Resource Schema",
    "description": "WorkflowResource envelope schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "WorkflowResource": {
        "type": "object",
        "required": [
          "apiVersion",
          "kind",
          "metadata",
          "spec"
        ],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": [
              "Workflow"
            ]
          },
          "metadata": {
            "allOf": [
              {
                "$ref": "./resource.json#/components/schemas/ResourceMetadata"
              }
            ],
            "description": "metadata.name is the workflow custom ID."
          },
          "spec": {
            "$ref": "../shared/workflow_spec.json#/components/schemas/WorkflowSpec"
          },
          "i18n": {
            "$ref": "../shared/workflow_i18n.json#/components/schemas/WorkflowI18n"
          },
          "icon": {
            "$ref": "../shared/icon.json#/components/schemas/Icon"
          }
        }
      }
    }
  }
}
