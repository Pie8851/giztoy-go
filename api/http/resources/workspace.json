{
  "openapi": "3.0.3",
  "info": {
    "title": "Workspace Resource Schema",
    "description": "WorkspaceResource envelope schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "WorkspaceResource": {
        "type": "object",
        "required": [
          "apiVersion",
          "kind",
          "metadata",
          "spec"
        ],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": [
              "Workspace"
            ]
          },
          "metadata": {
            "allOf": [
              {
                "$ref": "./resource.json#/components/schemas/ResourceMetadata"
              }
            ],
            "description": "metadata.name is the workspace custom ID. spec.workflow_name is the referenced workflow custom ID."
          },
          "spec": {
            "$ref": "../shared/workspace_spec.json#/components/schemas/WorkspaceSpec"
          },
          "icon": {
            "$ref": "../shared/icon.json#/components/schemas/Icon"
          }
        }
      }
    }
  }
}
