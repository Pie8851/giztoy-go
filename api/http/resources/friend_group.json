{
  "openapi": "3.0.3",
  "info": {
    "title": "Friend Group Resource Schema",
    "description": "FriendGroupResource envelope schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "FriendGroupResource": {
        "type": "object",
        "required": [
          "apiVersion",
          "kind",
          "metadata",
          "spec"
        ],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": [
              "FriendGroup"
            ]
          },
          "metadata": {
            "allOf": [
              {
                "$ref": "./resource.json#/components/schemas/ResourceMetadata"
              }
            ],
            "description": "metadata.name is the friend group custom ID."
          },
          "spec": {
            "$ref": "#/components/schemas/FriendGroupSpec"
          }
        }
      },
      "FriendGroupSpec": {
        "type": "object",
        "required": [
          "owner_public_key",
          "name"
        ],
        "properties": {
          "owner_public_key": {
            "type": "string",
            "minLength": 1,
            "description": "Immutable owner Peer public key used to resolve the system Workflow."
          },
          "name": {
            "type": "string",
            "description": "Display name for the friend group. metadata.name is the stable group id."
          },
          "description": {
            "type": "string"
          }
        }
      }
    }
  }
}
