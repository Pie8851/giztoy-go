{
  "openapi": "3.0.3",
  "info": {
    "title": "RegistrationToken Resource Schema",
    "description": "RegistrationToken declarative resource envelope.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "RegistrationTokenResource": {
        "type": "object",
        "required": ["apiVersion", "kind", "metadata", "spec"],
        "properties": {
          "apiVersion": {
            "$ref": "./resource.json#/components/schemas/ResourceAPIVersion"
          },
          "kind": {
            "type": "string",
            "enum": ["RegistrationToken"]
          },
          "metadata": {
            "$ref": "./resource.json#/components/schemas/ResourceMetadata"
          },
          "spec": {
            "type": "object",
            "required": ["token", "runtime_profile_name"],
            "additionalProperties": false,
            "properties": {
              "token": { "type": "string" },
              "runtime_profile_name": { "type": "string" },
              "firmware_id": {
                "type": "string",
                "description": "Optional Server-assigned Firmware release-line ID. The device selects its own channel."
              }
            }
          }
        }
      }
    }
  }
}
