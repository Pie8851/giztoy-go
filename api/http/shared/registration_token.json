{
  "openapi": "3.0.3",
  "info": {
    "title": "RegistrationToken API Types",
    "description": "RegistrationToken persisted resource.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "RegistrationToken": {
        "type": "object",
        "required": ["name", "token", "runtime_profile_name", "created_at", "updated_at"],
        "additionalProperties": false,
        "properties": {
          "name": { "type": "string" },
          "token": { "type": "string" },
          "runtime_profile_name": { "type": "string" },
          "firmware_id": {
            "type": "string",
            "description": "Optional Server-assigned Firmware release-line ID. The device selects its own channel."
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time"
          }
        }
      }
    }
  }
}
