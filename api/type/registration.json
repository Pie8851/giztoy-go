{
  "openapi": "3.0.3",
  "info": {
    "title": "Registration Type",
    "description": "Peer registration state schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "Registration": {
        "type": "object",
        "required": [
          "public_key",
          "role",
          "status",
          "created_at",
          "updated_at"
        ],
        "properties": {
          "public_key": {
            "type": "string"
          },
          "role": {
            "$ref": "./peer_role.json#/components/schemas/PeerRole"
          },
          "status": {
            "$ref": "./peer_registration_status.json#/components/schemas/PeerRegistrationStatus"
          },
          "auto_registered": {
            "type": "boolean"
          },
          "device": {
            "$ref": "./device_info.json#/components/schemas/DeviceInfo"
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time"
          },
          "approved_at": {
            "type": "string",
            "format": "date-time"
          }
        }
      }
    }
  }
}
