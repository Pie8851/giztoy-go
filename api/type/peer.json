{
  "openapi": "3.0.3",
  "info": {
    "title": "Peer Type",
    "description": "Peer schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "Peer": {
        "type": "object",
        "required": [
          "public_key",
          "role",
          "status",
          "device",
          "configuration",
          "created_at",
          "updated_at"
        ],
        "properties": {
          "public_key": {
            "type": "string"
          },
          "role": {
            "$ref": "./peer_role.json#/components/schemas/PeerRole"
          },
          "status": {
            "$ref": "./peer_registration_status.json#/components/schemas/PeerRegistrationStatus"
          },
          "device": {
            "$ref": "./device_info.json#/components/schemas/DeviceInfo"
          },
          "configuration": {
            "$ref": "./configuration.json#/components/schemas/Configuration"
          },
          "auto_registered": {
            "type": "boolean"
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time"
          },
          "approved_at": {
            "type": "string",
            "format": "date-time"
          }
        }
      }
    }
  }
}
