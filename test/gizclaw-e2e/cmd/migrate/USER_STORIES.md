# Migrate CLI

## User Story

As an operator, I want `gizclaw migrate --workspace` to prepare server workspace storage before serving.

## Covered Behaviors

- Migration runs through the real CLI binary.
- The migrated workspace can subsequently be served.
